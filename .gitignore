# Live configs carry real MACs, node IPs, and broker credentials — never commit them.
# Copy neewerd.example.toml to neewerd.toml (or a site name like luna.toml) and edit locally.
neewerd.toml
luna.toml
devices.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/

# OS
.DS_Store
